export function SiteFooter() {
  return (
    <footer className="border-t border-white/10 bg-black/20 text-[var(--text-muted)]">
      <div className="mx-auto flex min-h-16 w-full max-w-6xl flex-col items-center justify-center gap-1 px-5 py-4 text-center text-xs sm:flex-row sm:justify-between sm:px-8 sm:text-left">
        <p>MK Bet, ligue privée entre amis</p>
        <p>Aucun pari en argent réel</p>
      </div>
    </footer>
  );
}
